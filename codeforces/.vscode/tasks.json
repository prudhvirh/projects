{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "My build task",
            "type": "shell",
            "command": "/opt/homebrew/bin/g++-14 -std=c++17 -Wshadow -Wall ${file} && ./a.out",
            "options": {},
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build"
        }
    ]
}